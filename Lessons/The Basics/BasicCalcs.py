
Student_Grades = [20, 21, 22.5]

mysum = sum(Student_Grades)
mylength = len(Student_Grades)

Average = mysum / mylength 

print(Average)

#finding the mean of the data within a list, even though there is no mean function/method built into python
#cant use 'sum' as a variable name as sum is a function in python (that we are using also so thats pretty obvious)
#len is a function that returns the number of items in a container
